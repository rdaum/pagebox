[package]
name = "pagebox-swip-kernel"
version = "0.1.0"
edition = "2024"
license.workspace = true

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(loom)', 'cfg(miri)'] }

[dependencies]

[dev-dependencies]
loom.workspace = true
